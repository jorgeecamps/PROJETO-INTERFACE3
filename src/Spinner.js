// Spinner.js

import React from 'react';
import './Spinner.css'; // Importe o arquivo CSS do spinner

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
}

export default Spinner;
